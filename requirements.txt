asgiref==3.11.1
Django>=5.2,<5.3
django-crispy-forms==1.14.0
django-mathfilters==1.0.0
django-unfold>=0.40,<1.0
pillow>=10.0
reportlab>=4.0,<5.0
sqlparse==0.5.5

# PostgreSQL
psycopg2-binary

# Security
django-ratelimit

# QZ Tray request signing (silent thermal-receipt printing)
cryptography>=42,<46

# Background tasks
celery[redis]
django-celery-beat>=2.5,<3.0
redis

# REST API
djangorestframework

# Audit logging
django-auditlog

# Production server
gunicorn

# Static files
whitenoise>=6.6,<7.0

# Error monitoring (optional — init is a no-op without SENTRY_DSN)
sentry-sdk[django]>=2.0

# ML (optional — trainers fall back to baselines if these aren't installed)
pandas>=2.0
scikit-learn>=1.3
prophet>=1.1
mlxtend>=0.23
holidays>=0.50   # accurate Kenya holiday dates (incl. Eid lunar drift); falls back to hardcoded table

